import React from "react"

import { Login } from "../components"

export const LoginContainer = () => {
  return (
    <Login>
      <Login.Title>Inscription ou connexion</Login.Title>
    </Login>
  )
}
